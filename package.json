{
  "name": "commitcraft",
  "displayName": "commitcraft",
  "publisher": "AayushChopra",
  "description": "Generate commit messages from staged difference using GPT and trained model",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.105.0"
  },
  "categories": [
    "Other"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "commitcraft.generateCommit",
        "title": "CommitCraft: Generate Commit Message"
      }
    ],
    "keybindings": [
      {
        "command": "commitcraft.generateCommit",
        "key": "ctrl+alt+g",
        "when": "scmFocus"
      }
    ],
    "configuration": {
      "title": "CommitCraft",
      "properties": {
        "commitcraft.geminiApiKey": {
          "type": "string",
          "default": "",
          "markdownDescription": "Google AI studio. Leave empty to use environment variable GEMINI_API_KEY."
        },
        "commitcraft.model": {
          "type": "string",
          "default": "gemini-2.5-flash",
          "markdownDescription": "Gemini Model name for generating commit messages."
        },
        "commitcraft.maxChars": {
          "type": "number",
          "default": 72,
          "markdownDescription": "Max characters for the subject line."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "compile": "npm run check-types && npm run lint && node esbuild.js",
    "watch": "npm-run-all -p watch:*",
    "watch:esbuild": "node esbuild.js --watch",
    "watch:tsc": "tsc --noEmit --watch --project tsconfig.json",
    "package": "npm run check-types && npm run lint && node esbuild.js --production",
    "compile-tests": "tsc -p . --outDir out",
    "watch-tests": "tsc -p . -w --outDir out",
    "pretest": "npm run compile-tests && npm run compile && npm run lint",
    "check-types": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "^22.19.1",
    "@types/vscode": "^1.105.0",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.5.2",
    "esbuild": "^0.27.0",
    "eslint": "^9.39.1",
    "npm-run-all": "^4.1.5",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.46.3"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "dotenv": "^17.2.3",
    "openai": "^6.9.0"
  }
}
